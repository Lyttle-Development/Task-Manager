import * as Imports from './imports';
import { dashboardPrefix } from '@lyttledev-dashboard/utils';

export const componentsPrefix = dashboardPrefix + 'components.';

export const Component = Imports;
export default Imports;
